apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ .Release.Name }}-node-ds
  namespace: {{ .Release.Namespace }}
  labels:
    tier: node
    app.kubernetes.io/created-by: dpf-operator
    app.kubernetes.io/part-of: dpf-operator
  {{- include "sfc-controller.labels" . | nindent 4 }}
  annotations:
    {{- include "sfc-controller.annotations" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      name: sfc-controller-node
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        tier: node
        app: sfc-controller-node
        name: sfc-controller-node
        {{- include "sfc-controller.labels" . | nindent 8 }}
      annotations:
        {{- include "sfc-controller.annotations" . | nindent 8 }}
    spec:
      automountServiceAccountToken: false
      tolerations:
        - key: node-role.kubernetes.io/master
          operator: Exists
          effect: NoSchedule
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
      serviceAccountName: {{ include "sfc-controller.fullname" . }}-node
      hostNetwork: true
      securityContext:
        runAsUser: 0
      containers:
      - args:
        - --secure-flow-deletion-timeout={{ .Values.controllerManager.manager.secureFlowDeletionTimeout }}
        - --metrics-bind-address=:{{ .Values.controllerManager.manager.ports.metrics }}
        - --health-probe-bind-address=:{{ .Values.controllerManager.manager.ports.health }}
        {{- toYaml .Values.controllerManager.manager.args | nindent 8 }}
        command:
        - /sfc-controller
        env:
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        - name: SFC_NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: SYSTEMCTL_FORCE_BUS
          value: "1"
        volumeMounts:
        - mountPath: /var/run/secrets/kubernetes.io/serviceaccount
          name: kube-api-access
          readOnly: true
        - name: ovs
          mountPath: /var/run/openvswitch/
        - name: ovs-systemd-config
          mountPath: /etc/default/openvswitch-switch
        - name: var-run-systemd
          mountPath: /var/run/systemd
        - name: systemd-dbus
          mountPath: /var/run/dbus/system_bus_socket
        - mountPath: /lib
          name: lib
        {{- if .Values.openvSwitchSharedLibrary64Dir }}
        - mountPath: /lib64
          name: lib64
        {{- end }}
        - mountPath: /usr/bin/ovs-vsctl
          name: ovs-vsctl
        - mountPath: /usr/bin/ovs-ofctl
          name: ovs-ofctl
        {{- if .Values.dpuLinkerCachePath }}
        - mountPath: {{ .Values.dpuLinkerCachePath }}
          name: dpu-ld-cache
          readOnly: true
        {{- end }}
        {{- if .Values.dpuOptLibraryPath }}
        - mountPath: {{ .Values.dpuOptLibraryPath }}
          name: dpu-opt-lib
          readOnly: true
        {{- end }}
        image: {{ .Values.controllerManager.manager.image.repository }}:{{ .Values.controllerManager.manager.image.tag
          | default .Chart.AppVersion }}
        securityContext:
          privileged: true
        livenessProbe:
          httpGet:
            path: /healthz
            port: health
          initialDelaySeconds: 15
          periodSeconds: 20
        name: sfc-controller
        readinessProbe:
          httpGet:
            path: /readyz
            port: health
          initialDelaySeconds: 5
          periodSeconds: 10
        resources: {{- toYaml .Values.controllerManager.manager.resources | nindent 10
          }}
        securityContext: {{- toYaml .Values.controllerManager.manager.containerSecurityContext
          | nindent 10 }}
        ports:
          - name: metrics
            containerPort: {{ .Values.controllerManager.manager.ports.metrics }}
            protocol: TCP
          - name: health
            containerPort: {{ .Values.controllerManager.manager.ports.health }}
            protocol: TCP
      imagePullSecrets: {{ .Values.imagePullSecrets | default list | toJson }}
      terminationGracePeriodSeconds: 10
      volumes:
      - name: kube-api-access
        projected:
          defaultMode: 420
          sources:
          - serviceAccountToken:
              path: token
          - configMap:
              items:
              - key: ca.crt
                path: ca.crt
              name: kube-root-ca.crt
          - downwardAPI:
              items:
              - fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.namespace
                path: namespace
      ## TODO: Revisit if ovs-systemd-config is needed.
      - name: ovs-systemd-config
        hostPath:
          path: /etc/default/openvswitch-switch
      ## TODO: Revisit if var-run-systemd is needed.
      - name: var-run-systemd
        hostPath:
          path: /var/run/systemd
      ## TODO: Revisit if systemd-dbus is needed.
      - name: systemd-dbus
        hostPath:
          path: /var/run/dbus/system_bus_socket
      - name: ovs
        hostPath:
          path: {{ .Values.openvSwitchRunDir }}
      - hostPath:
          path: {{ .Values.openvSwitchBinDir }}/ovs-vsctl
          type: File
        name: ovs-vsctl
      - hostPath:
          path: {{ .Values.openvSwitchBinDir }}/ovs-ofctl
          type: File
        name: ovs-ofctl
      - hostPath:
          path: {{ .Values.openvSwitchSharedLibraryDir }}
          type: Directory
        name: lib
      {{- if .Values.openvSwitchSharedLibrary64Dir }}
      - hostPath:
          path: {{ .Values.openvSwitchSharedLibrary64Dir }}
          type: Directory
        name: lib64
      {{- end }}
      {{- if .Values.dpuLinkerCachePath }}
      - hostPath:
          path: {{ .Values.dpuLinkerCachePath }}
          type: File
        name: dpu-ld-cache
      {{- end }}
      {{- if .Values.dpuOptLibraryPath }}
      - hostPath:
          path: {{ .Values.dpuOptLibraryPath }}
          type: DirectoryOrCreate
        name: dpu-opt-lib
      {{- end }}

